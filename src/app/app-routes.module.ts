import { NgModule } from '@angular/core';
import { Route } from '@angular/router';

// const appRoutes: Route = [
//   { path: 'login', component: ProductItemComponent }
// ];

@NgModule({
  declarations: [],
  imports: [
  ]
})
export class AppRoutesModule { }
